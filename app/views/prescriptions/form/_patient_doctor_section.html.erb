<div class="space-y-6 sm:space-y-5">
    <div>
      <h3 class="text-lg font-medium leading-6 text-gray-900">Prescription Details</h3>
    </div>
  
    <div class="space-y-6 sm:space-y-5">
      <%= render "prescriptions/form/select_field", 
        f: f,
        field: :patient_id,
        collection: patients,
        prompt: "Select a patient" %>
  
      <%= render "prescriptions/form/select_field",
        f: f, 
        field: :doctor_id,
        collection: doctors,
        prompt: "Select a doctor" %>
    </div>
</div>